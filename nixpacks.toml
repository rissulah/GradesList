[phases.setup]
nixPkgs = ["nodejs"]

[phases.build]
cmds = ["composer install --no-dev", "npm install", "npm run build", "php artisan config:cache"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"